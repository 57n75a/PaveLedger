# Source provenance

Export prepared 25 September 2026 for the PaveLedger project.

- Saved Sites project: `appgprj_6ab67f65f7c88191b3a47a78fecaedab`.
- Saved source commit: `b45dacf818f1f636f300e9f160440ad5b79b37ad`.
- The master package contains an unchanged Git archive of that source under originals.
- The earlier PaveLedger_Vercel_Deployment.zip was truncated within a PNG and lacked its ZIP central directory. Ninety earlier complete local entries were recovered with CRC validation. It is retained, clearly labelled DAMAGED_REFERENCE, only in the master package.
- This v2.0 app uses the recovered Vercel conversion and restores missing public assets, vendor styles and TypeScript configuration from the saved source. It adds revised roles, memberships, contracts, private evidence, verification gates and workflow tests.
- The source project's Cloudflare/D1 runtime and ChatGPT identity are replaced here by standard Next.js on Vercel plus Supabase Auth/PostgreSQL/Storage.

The live D1 viewer exposed the workspaces schema and one row but truncated its data field. No complete live-data export, Auth account export, secrets or credentials are included. No site settings or deployed source were changed.

The master ZIP preserves 13 identified PaveLedger file artifacts plus the saved site source and supplementary files extracted from the original complete package. It does not claim to cover inaccessible conversations or unlisted historical artifacts.
